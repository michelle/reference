[
  {
    "name": "Peer",
    "type": "constructor",
    "snippet": "var peer = new Peer([id], [options]);",
    "decription": "A Peer can connect to other peers and listen for connections.",
    "children": [
      {
        "name": "id",
        "optional": true,
        "type": "string",
        "description": "Other peers can connect to this peer using the provided ID. If no ID is given, one will be generated by the brokering server.<br>It is not recommended that you use this ID to identify peers, as it's meant to be used for brokering connections only. You're recommended to set the metadata option to send other identifying information."
      },
      {
        "name": "options",
        "optional": true,
        "type": "object",
        "children": [
          {
            "name": "key",
            "type": "string",
            "description": "API key for the cloud PeerServer. This is not used for servers other than 0.peerjs.com."
          },
          {
            "name": "host",
            "type": "string",
            "description": "Server host. Defaults to 0.peerjs.com. Also accepts '/' to signify relative hostname."
          }
        ]
      }
    ]
  },

  {
    "name": "peer.connect",
    "type": "method",
    "snippet": "var connection = peer.connect(id, [options]);",
    "description": "Does something",
    "children": [
      {
        "name": "id",
        "type": "string",
        "description": "Something else"
      }
    ]
  }
]
